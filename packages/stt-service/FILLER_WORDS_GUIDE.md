# 🧹 Руководство по фильтрации слов-паразитов

## Проблема

STT системы **распознают** слова-паразиты и лишние звуки:
- "Эээ", "ммм", "а-а-а"
- "Ну", "вот", "типа", "короче"
- "В общем", "в принципе", "это самое"
- Повторы: "я я", "и и"

**Пример:**
```
Исходное аудио: "Эээ ну вот я думаю что эээ это хорошо"
STT распознаёт: "Эээ ну вот я думаю что эээ это хорошо" ✅ (точно!)
Нужен результат: "Я думаю что это хорошо" 🎯
```

---

## ✅ Решения

### 1. **Yandex API: параметр `literature_text`**

Встроенная фильтрация от Yandex:

```bash
python3 transcribe.py audio.mp3 --literature
```

**Что делает:**
- Убирает "эээ", "ммм", звуки
- Убирает некоторые слова-паразиты
- Делает текст более литературным

**Плюсы:** Работает на уровне API, быстро  
**Минусы:** Не все паразиты убираются

---

### 2. **Post-processing фильтр (наш)**

Более агрессивная очистка после транскрибации:

```bash
python3 transcribe.py audio.mp3 --clean
```

**Что убирает:**
- Звуки: "эээ", "ммм", "а-а", "у-у"
- Паразиты: "ну", "вот", "типа", "короче", "блин"
- Вводные: "в общем", "в принципе", "это самое"
- Лишние: "вообще", "наверное", "кстати"
- Повторы: "я я" → "я"

---

### 3. **Комбинация (лучший результат)**

```bash
python3 transcribe.py audio.mp3 --literature --clean -v
```

**Результат:**
1. Yandex убирает звуки на уровне API
2. Наш фильтр дочищает слова-паразиты
3. Максимально чистый текст!

---

## 📊 Примеры

### Пример 1: Звуки

```bash
# Исходное аудио
"Эээ ммм я думаю что это хорошо"

# Без фильтров
python3 transcribe.py audio.mp3
→ "Эээ ммм я думаю что это хорошо"

# С --literature
python3 transcribe.py audio.mp3 --literature
→ "Я думаю что это хорошо"

# С --clean
python3 transcribe.py audio.mp3 --clean
→ "Я думаю что это хорошо"
```

### Пример 2: Слова-паразиты

```bash
# Исходное аудио
"Ну вот типа я короче думаю что блин это хорошо"

# Без фильтров
→ "Ну вот типа я короче думаю что блин это хорошо"

# С --clean
→ "Я думаю что это хорошо"
```

### Пример 3: Комплексный

```bash
# Исходное аудио
"Эээ ну в общем я я думаю что вот это самое хорошо наверное"

# С --literature --clean
→ "Я думаю что это хорошо"
```

---

## 🎯 Когда использовать

### --literature (Yandex API)

**Используй когда:**
- Нужна быстрая очистка
- Достаточно убрать звуки ("эээ", "ммм")
- Важна скорость

**Пример:** Субтитры для видео

### --clean (Post-processing)

**Используй когда:**
- Нужна глубокая очистка
- Много слов-паразитов
- Текст для документации/отчётов

**Пример:** Протоколы совещаний, интервью

### --literature --clean (Оба)

**Используй когда:**
- Нужен максимально чистый текст
- Профессиональная транскрипция
- Публикация материалов

**Пример:** Статьи, книги, официальные документы

---

## ⚙️ Настройка фильтра

### Добавить свои слова-паразиты

**Файл:** `filler_words_filter.py`

```python
self.russian_fillers = [
    r'\bэ+\b',
    r'\bну\b',
    # Добавь свои:
    r'\bблять\b',
    r'\bхуле\b',
]
```

### Python API

```python
from yandex_stt import YandexSTT
from filler_words_filter import FillerWordsFilter

stt = YandexSTT()
filler_filter = FillerWordsFilter()

# Транскрибация с literature_text
result = stt.transcribe_sync(
    "audio.mp3",
    format='oggopus',
    literature_text=True
)

text = result['result']

# Дополнительная очистка
text = filler_filter.clean(text)

print(text)
```

---

## 🔧 Дополнительные опции

### Агрессивная очистка

```python
from filler_words_filter import FillerWordsFilter

filter = FillerWordsFilter()

text = "Эээ я я думаю э что это хорошо"

# Обычная
clean = filter.clean(text)
print(clean)  # "Я думаю что это хорошо"

# Агрессивная (убирает даже "я")
aggressive = filter.clean(text, aggressive=True)
print(aggressive)  # "Думаю что это хорошо"
```

**Осторожно:** Агрессивная очистка может убрать нужные слова!

### Добавить кастомный паттерн

```python
filter = FillerWordsFilter()

# Добавить новое слово-паразит
filter.add_filler(r'\bбля\b')
filter.add_filler(r'\bзнаете\b')

text = "Бля знаете это хорошо"
clean = filter.clean(text)
print(clean)  # "Это хорошо"
```

---

## 📋 Полный список слов-паразитов

### Звуки
- эээ, ммм, ааа, ууу
- эм, ам, ум
- м-м-м, э-э-э

### Русские слова-паразиты
- ну, вот, типа, короче, блин
- в общем, в принципе, это самое
- вообще, наверное, кстати
- знаешь, понимаешь, видишь ли
- собственно, допустим
- то есть

### Английские (если нужно)
- um, uh, er, ah
- like, you know, I mean
- actually, basically, literally
- kinda, sorta

### Повторы
- "я я" → "я"
- "и и" → "и"
- "что что" → "что"

---

## 💡 Советы

### 1. Качество записи важнее фильтров

**Лучше:**
- Говорить медленно и чётко
- Избегать слов-паразитов при записи
- Использовать хороший микрофон

**Чем:**
- Записать "как получилось"
- Потом чистить фильтрами

### 2. Тестируй на разных данных

```bash
# Протестируй без фильтров
python3 transcribe.py audio.mp3 -o raw.txt

# С фильтрами
python3 transcribe.py audio.mp3 --literature --clean -o clean.txt

# Сравни
diff raw.txt clean.txt
```

### 3. Выбери баланс

Слишком агрессивная фильтрация может убрать нужные слова!

**Пример:**
```
Исходно: "Ну а если вообще подумать наверное это хорошо"
Очищено: "Если подумать это хорошо"  ✅
Слишком: "Это хорошо"  ⚠️ (потеряли смысл)
```

---

## 🚀 Быстрый старт

### Шаг 1: Базовая очистка

```bash
python3 transcribe.py audio.mp3 --clean
```

### Шаг 2: Максимальная очистка

```bash
python3 transcribe.py audio.mp3 --literature --clean -v
```

### Шаг 3: С исправлениями

```bash
python3 transcribe.py audio.mp3 --literature --clean --corrections my.json -v
```

---

## 📊 Сравнение методов

| Метод | Скорость | Качество очистки | Риск потери данных |
|-------|----------|------------------|-------------------|
| Без фильтров | ⭐⭐⭐ | - | - |
| --literature | ⭐⭐⭐ | ⭐⭐ | Низкий |
| --clean | ⭐⭐ | ⭐⭐⭐⭐ | Средний |
| Оба | ⭐⭐ | ⭐⭐⭐⭐⭐ | Средний |

---

## ✅ Итог

**Лучшая практика:**

```bash
python3 transcribe.py audio.mp3 \
  --literature \              # Yandex убирает звуки
  --clean \                   # Наш фильтр убирает слова
  --corrections my.json \     # Исправления специфичных слов
  -v \                        # Подробный вывод
  -o result.txt               # Сохранить
```

**Результат:**
- ✅ Нет звуков ("эээ", "ммм")
- ✅ Нет слов-паразитов ("ну", "вот", "типа")
- ✅ Правильные специфичные слова ("ИИшка", "СПРИНТ")
- ✅ Чистый профессиональный текст!

---

*Настроено by Aquilla 🦅*
