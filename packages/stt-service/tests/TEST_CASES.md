# Детальные тест-кейсы

## TC-001: Загрузка стандартного MP3 файла

**Приоритет:** Высокий  
**Тип:** Функциональный  
**Предусловия:** Сервис запущен, пользователь на главной странице

**Шаги:**
1. Подготовить MP3 файл размером 2-5 МБ с русской речью
2. Открыть http://92.51.36.233:8000
3. Нажать на зону загрузки или перетащить файл
4. Убедиться что все чекбоксы в дефолтном состоянии (Punctuation=ON, Corrections=ON)
5. Нажать "Начать транскрибацию"
6. Дождаться завершения обработки

**Ожидаемый результат:**
- Файл принят без ошибок
- Progress bar появился и обновляется 0% → 100%
- Показываются статусы: "Конвертация...", "Транскрибация...", etc.
- Через 30-120 секунд появляется результат транскрибации
- Текст на русском языке с пунктуацией
- Кнопки "Копировать" и "Скачать TXT" работают
- Задача появилась в истории со статусом "Готово"

**Критерии прохождения:**
- Транскрибация завершена успешно
- Текст соответствует аудио (проверить вручную)
- Нет ошибок в консоли браузера

---

## TC-002: Загрузка AAC файла с диктофона

**Приоритет:** Высокий  
**Тип:** Функциональный  
**Предусловия:** Наличие AAC файла с iPhone/Android диктофона

**Шаги:**
1. Записать голосовую заметку на телефоне (30-60 секунд)
2. Перенести AAC файл на компьютер
3. Загрузить через веб-интерфейс
4. Дождаться обработки

**Ожидаемый результат:**
- AAC файл принимается без ошибок MIME type
- Успешная конвертация в OGG Opus
- Транскрибация завершается успешно
- Качество текста приемлемое

**Известные проблемы:**
- AAC может определяться как text/plain MIME - должно обрабатываться корректно

---

## TC-003: Chunked upload большого файла (30+ МБ)

**Приоритет:** Высокий  
**Тип:** Функциональный + Performance

**Шаги:**
1. Подготовить аудиофайл размером 30-50 МБ
2. Открыть DevTools (F12) → Network
3. Загрузить файл через интерфейс
4. Наблюдать за Network requests

**Ожидаемый результат:**
- В Network видны множественные POST /upload-chunk запросы
- Каждый chunk ~5 МБ
- Progress bar показывает "часть X/Y"
- После последнего чанка идёт POST /complete-upload
- Файл успешно собирается на сервере
- Транскрибация запускается

**Метрики:**
- Скорость загрузки отображается (МБ/с)
- ETA отображается и обновляется
- Нет "Failed to fetch" ошибок
- Общее время загрузки 30 МБ: < 5 минут на 10 Мбит/с

---

## TC-004: Rate Limiting

**Приоритет:** Средний  
**Тип:** Безопасность

**Шаги:**
1. Подготовить небольшой тестовый файл (1 МБ)
2. Быстро загрузить файл 11 раз подряд
3. Зафиксировать ответ на 11-й запрос

**Ожидаемый результат:**
- Первые 10 запросов проходят успешно
- 11-й запрос отклоняется с HTTP 429
- Сообщение: "Слишком много запросов. Максимум 10 запросов в минуту."
- Через 61 секунду счётчик сбрасывается

**Тестовый скрипт:**
```bash
for i in {1..11}; do
  curl -X POST http://92.51.36.233:8000/transcribe \
    -F "file=@test.mp3" \
    -w "\nRequest $i: %{http_code}\n"
  sleep 1
done
```

---

## TC-005: XSS через имя файла

**Приоритет:** Критичный  
**Тип:** Безопасность

**Шаги:**
1. Создать файл с именем: `<script>alert('XSS')</script>.mp3`
2. Загрузить через интерфейс
3. Проверить History секцию
4. Проверить HTML источник страницы

**Ожидаемый результат:**
- Имя файла экранируется в HTML
- `<script>` отображается как текст, не выполняется
- В HTML видно: `&lt;script&gt;alert('XSS')&lt;/script&gt;`
- JavaScript не выполнился

**Негативный результат (FAIL):**
- Alert всплывает
- JavaScript выполняется
- HTML содержит неэкранированные `<script>` теги

---

## TC-006: Path Traversal через имя файла

**Приоритет:** Критичный  
**Тип:** Безопасность

**Шаги:**
1. Создать файл с именем: `../../../etc/passwd.mp3`
2. Попытаться загрузить
3. Проверить логи сервера
4. Проверить что файл сохранён в правильной директории

**Ожидаемый результат:**
- Имя санитизируется до безопасного варианта
- Файл сохраняется в `/app/uploads/` с безопасным именем
- Нет доступа к `/etc/passwd` или другим системным путям
- В History отображается санитизированное имя

**Проверка:**
```bash
# На сервере
ls -la /app/uploads/
# Не должно быть файлов вне этой директории
```

---

## TC-007: Пустой файл (0 байт)

**Приоритет:** Средний  
**Тип:** Валидация

**Шаги:**
1. Создать пустой файл: `touch empty.mp3`
2. Попытаться загрузить
3. Проверить сообщение об ошибке

**Ожидаемый результат:**
- Файл отклоняется с HTTP 400
- Сообщение: "Файл пустой"
- Файл не сохраняется на сервере
- Понятное объяснение в UI

---

## TC-008: Файл ровно 500 МБ (граничное значение)

**Приоритет:** Средний  
**Тип:** Граничные условия

**Шаги:**
1. Создать файл ровно 500 МБ (524,288,000 байт)
2. Попытаться загрузить
3. Проверить принимается ли файл

**Ожидаемый результат:**
- Файл принимается (500 МБ ≤ MAX_FILE_SIZE)
- Chunked upload используется
- Загрузка завершается успешно

**Граничные случаи:**
- 499 МБ → принимается ✅
- 500 МБ → принимается ✅
- 501 МБ → отклоняется ❌ "Файл слишком большой"

---

## TC-009: Загрузка неподдерживаемого формата

**Приоритет:** Средний  
**Тип:** Негативный тест

**Тестовые файлы:**
- `document.pdf` (PDF документ)
- `photo.jpg` (изображение)
- `archive.zip` (архив)
- `script.exe` (исполняемый файл)

**Ожидаемый результат для каждого:**
- HTTP 400 Bad Request
- Сообщение содержит объяснение: "не поддерживается" или "не является аудио"
- Файл удаляется с сервера
- В History ничего не добавляется

---

## TC-010: Битый/повреждённый аудиофайл

**Приоритет:** Средний  
**Тип:** Обработка ошибок

**Шаги:**
1. Создать файл с расширением `.mp3` но случайным содержимым:
   ```bash
   dd if=/dev/urandom of=corrupted.mp3 bs=1024 count=100
   ```
2. Загрузить через интерфейс
3. Дождаться обработки

**Ожидаемый результат:**
- Файл принимается (проходит валидацию расширения)
- Начинается конвертация через ffmpeg
- ffmpeg не может обработать → ошибка
- Задача помечается как "failed"
- Сообщение: "Файл повреждён или имеет неподдерживаемый формат"
- Детали в консоли (F12)

---

## TC-011: Одновременная загрузка 3 файлов

**Приоритет:** Средний  
**Тип:** Конкурентность

**Шаги:**
1. Открыть 3 вкладки браузера с сервисом
2. В каждой загрузить разный файл одновременно
3. Следить за обработкой

**Ожидаемый результат:**
- 3 задачи создаются с разными task_id
- Первые 3 обрабатываются параллельно (3 воркера)
- Остальные ждут в очереди
- Все завершаются успешно
- History показывает все 3 задачи

**Метрики:**
- Статистика: queued=0, processing=3 → processing=2 → ...
- Нет race conditions
- Нет перепутанных результатов

---

## TC-012: Прерывание загрузки (закрытие вкладки)

**Приоритет:** Низкий  
**Тип:** Edge case

**Шаги:**
1. Начать загрузку большого файла (50 МБ)
2. Во время загрузки (30-50%) закрыть вкладку браузера
3. Проверить сервер

**Ожидаемый результат:**
- Частично загруженные чанки остаются в `/app/chunks/`
- Задача не создаётся (не был вызван /complete-upload)
- Нет упавших воркеров
- Временные файлы можно очистить через cleanup endpoint

**Будущее улучшение:**
- Автоочистка неполных загрузок старше 1 часа

---

## TC-013: Unicode в имени файла

**Приоритет:** Средний  
**Тип:** Интернационализация

**Тестовые имена:**
- `запись_интервью_2024.mp3` (кириллица)
- `会议录音.mp3` (китайские иероглифы)
- `مقابلة.mp3` (арабский)
- `🎤_podcast_😊.mp3` (emoji)

**Ожидаемый результат:**
- Имена санитизируются безопасно
- Кириллица сохраняется
- Emoji и спецсимволы заменяются на `_`
- Файл сохраняется и отображается корректно
- Скачивание работает с санитизированным именем

---

## TC-014: Очень длинное имя файла

**Приоритет:** Низкий  
**Тип:** Граничные условия

**Шаги:**
1. Создать файл с именем 500 символов
2. Загрузить

**Ожидаемый результат:**
- Имя обрезается до 200 символов
- Расширение сохраняется
- Файл обрабатывается нормально

---

## TC-015: Проверка логирования

**Приоритет:** Средний  
**Тип:** Мониторинг

**Шаги:**
1. Загрузить файл
2. Запросить логи: `GET /logs?lines=50`
3. Проверить содержимое

**Ожидаемый результат в логах:**
- Timestamp для каждой записи
- IP адрес клиента
- Имя файла и размер
- "Upload request from X.X.X.X: filename.mp3 (audio/mpeg)"
- "File validated successfully: filename.mp3"
- Статусы обработки
- При ошибках - traceback

---

## Сводная таблица тест-кейсов

| ID | Название | Приоритет | Автоматизация | Статус |
|---|---|---|---|---|
| TC-001 | Стандартный MP3 | Высокий | ✅ Да | 🟢 Pass |
| TC-002 | AAC с диктофона | Высокий | ⚠️ Частично | 🟡 Нужна проверка |
| TC-003 | Chunked upload 30MB | Высокий | ✅ Да | 🟢 Pass |
| TC-004 | Rate limiting | Средний | ✅ Да | 🟢 Pass |
| TC-005 | XSS имя файла | Критичный | ✅ Да | 🟢 Pass |
| TC-006 | Path traversal | Критичный | ✅ Да | 🟢 Pass |
| TC-007 | Пустой файл | Средний | ✅ Да | 🟢 Pass |
| TC-008 | 500 МБ граница | Средний | ⚠️ Ручной | ⚪ Не протестирован |
| TC-009 | Неподд. формат | Средний | ✅ Да | 🟢 Pass |
| TC-010 | Битый файл | Средний | ⚠️ Ручной | ⚪ Не протестирован |
| TC-011 | 3 одновременно | Средний | ⚠️ Ручной | ⚪ Не протестирован |
| TC-012 | Прерывание | Низкий | ❌ Нет | ⚪ Не протестирован |
| TC-013 | Unicode имя | Средний | ⚠️ Ручной | ⚪ Не протестирован |
| TC-014 | Длинное имя | Низкий | ✅ Да | ⚪ Не протестирован |
| TC-015 | Логирование | Средний | ✅ Да | 🟢 Pass |

**Легенда:**
- 🟢 Pass - тест пройден
- 🔴 Fail - тест провален  
- 🟡 Flaky - нестабильный результат
- ⚪ Not tested - не протестирован
