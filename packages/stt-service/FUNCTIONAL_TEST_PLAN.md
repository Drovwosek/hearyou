# Функциональный Тест-План — STT Сервис

**Версия:** 1.0  
**Дата:** 2026-02-26  
**Ответственный:** Аквилла  

---

## 1. Цель Тестирования

Проверить что **все заявленные функции** STT сервиса работают корректно в типичных и граничных сценариях.

**Критерии успеха:**
- 100% критичных тестов пройдено
- 95%+ высокоприоритетных тестов пройдено
- Все основные user flow работают end-to-end

---

## 2. Scope (Что Тестируем)

### В тестировании:
✅ Загрузка файлов (все поддерживаемые форматы)  
✅ Обработка и транскрибация  
✅ API endpoints (POST, GET)  
✅ Опции транскрибации (литературный текст, очистка, исправления)  
✅ Chunked upload для больших файлов  
✅ История задач и статистика  
✅ Скачивание результатов (TXT)  
✅ UI взаимодействие (drag-drop, кнопки, статусы)  
✅ Обработка ошибок (валидация, недопустимые форматы)  

### Не тестируем (отдельные планы):
❌ Performance (скорость, нагрузка)  
❌ Security (injections, XSS)  
❌ UX/UI дизайн  
❌ Инфраструктура (Docker, CI/CD)  

---

## 3. Тест-Кейсы

### Категория A: Базовая Транскрибация

| ID | Приоритет | Описание | Входные данные | Ожидаемый результат |
|----|-----------|----------|----------------|---------------------|
| FT-001 | Критичный | Транскрибация MP3 | `test.mp3` (1 МБ, русская речь) | Текст получен, совпадает с содержимым, статус "completed" |
| FT-002 | Критичный | Транскрибация AAC | `test.aac` (230 КБ, с диктофона) | Текст получен корректно |
| FT-003 | Высокий | Транскрибация WAV | `test.wav` (5 МБ, несжатый) | Конвертация в Opus → текст |
| FT-004 | Высокий | Транскрибация FLAC | `test.flac` (10 МБ, lossless) | Корректная обработка |
| FT-005 | Высокий | Транскрибация OGG Opus | `test.opus` (500 КБ) | Прямая обработка без конвертации |
| FT-006 | Средний | Транскрибация M4A | `test.m4a` (2 МБ) | Корректный текст |
| FT-007 | Средний | Транскрибация AMR | `test.amr` (старый формат мобильных) | Поддержка legacy формата |

### Категория B: Видео Форматы (Извлечение Аудио)

| ID | Приоритет | Описание | Входные данные | Ожидаемый результат |
|----|-----------|----------|----------------|---------------------|
| FT-008 | Критичный | MP4 видео | `video.mp4` (10 МБ, с аудио) | Аудио извлечено, транскрибация успешна |
| FT-009 | Высокий | MKV видео | `video.mkv` (15 МБ) | Корректная обработка |
| FT-010 | Средний | AVI видео | `video.avi` (20 МБ) | Legacy формат поддержан |
| FT-011 | Средний | MOV видео | `video.mov` (8 МБ) | Apple формат работает |
| FT-012 | Низкий | WEBM видео | `video.webm` (5 МБ) | Web формат поддержан |

### Категория C: Опции Транскрибации

| ID | Приоритет | Описание | Входные данные | Ожидаемый результат |
|----|-----------|----------|----------------|---------------------|
| FT-013 | Критичный | Литературный текст | `test.mp3` + `literature=true` | Убраны "эээ", "ммм", звуки |
| FT-014 | Критичный | Очистка слов-паразитов | `test.mp3` + `clean=true` | Убраны "ну", "вот", "типа", "короче" |
| FT-015 | Высокий | Кастомные исправления | `test.mp3` + `corrections={"ИИшка":"AI"}` | "ИИшка" → "AI" в тексте |
| FT-016 | Высокий | Комбинация опций | `test.mp3` + все опции включены | Чистый профессиональный текст |
| FT-017 | Средний | Выбор языка | `test_en.mp3` + `language=en-US` | Английская транскрибация |

### Категория D: Большие Файлы и Chunked Upload

| ID | Приоритет | Описание | Входные данные | Ожидаемый результат |
|----|-----------|----------|----------------|---------------------|
| FT-018 | Критичный | Chunked upload 30 МБ | `large.mp3` (30 МБ) | Файл собран, транскрибация успешна |
| FT-019 | Критичный | Chunked upload 50 МБ | `large.wav` (50 МБ) | Все чанки получены, файл валиден |
| FT-020 | Высокий | Chunked upload 100 МБ | `huge.mp4` (100 МБ) | Успешная обработка |
| FT-021 | Высокий | Граничное значение 499 МБ | `max_size.mp4` (499 МБ) | Принят и обработан |
| FT-022 | Средний | Файл ровно 10 МБ | `threshold.mp3` (10.0 МБ) | Выбор между chunked/standard работает |

### Категория E: API Endpoints

| ID | Приоритет | Описание | HTTP запрос | Ожидаемый ответ |
|----|-----------|----------|-------------|-----------------|
| FT-023 | Критичный | POST /transcribe | multipart form-data с `file` | `{"task_id": "...", "status": "pending"}` |
| FT-024 | Критичный | GET /status/{task_id} | валидный task_id | `{"status": "pending"/"processing"/"completed"}` |
| FT-025 | Критичный | GET /result/{task_id} | task_id завершённой задачи | `{"text": "...", "status": "completed"}` |
| FT-026 | Высокий | GET /download/{task_id} | task_id завершённой задачи | TXT файл с корректным именем |
| FT-027 | Высокий | GET /stats | нет параметров | JSON с `{"total_tasks", "completed", "failed"}` |
| FT-028 | Высокий | GET /history | нет параметров | Массив задач с метаданными |
| FT-029 | Средний | GET /formats | нет параметров | Список поддерживаемых форматов |
| FT-030 | Средний | GET /logs?lines=50 | query param `lines` | Последние 50 строк логов |

### Категория F: Chunked Upload API

| ID | Приоритет | Описание | HTTP запрос | Ожидаемый ответ |
|----|-----------|----------|-------------|-----------------|
| FT-031 | Критичный | POST /upload-chunk (первый) | chunk 0/5 + metadata | `{"upload_id": "...", "received": 1}` |
| FT-032 | Критичный | POST /upload-chunk (средний) | chunk 2/5 | `{"received": 3}` |
| FT-033 | Критичный | POST /upload-chunk (последний) | chunk 4/5 | `{"received": 5}` |
| FT-034 | Критичный | POST /complete-upload | upload_id после всех чанков | `{"task_id": "...", "status": "pending"}` |
| FT-035 | Высокий | Complete без всех чанков | пропущен chunk 2 | Ошибка "недостаточно чанков" |

### Категория G: Валидация и Ошибки

| ID | Приоритет | Описание | Входные данные | Ожидаемый результат |
|----|-----------|----------|----------------|---------------------|
| FT-036 | Критичный | Пустой файл (0 байт) | `empty.mp3` (0 bytes) | Ошибка "Файл пустой" |
| FT-037 | Критичный | Файл > 500 МБ | `too_large.mp4` (501 МБ) | Ошибка "Превышен лимит" |
| FT-038 | Критичный | Недопустимый формат | `document.txt` | Ошибка "Неподдерживаемый формат" |
| FT-039 | Критичный | Битый/повреждённый файл | `corrupted.mp3` (invalid data) | Ошибка валидации ffprobe |
| FT-040 | Высокий | Видео без аудио | `silent_video.mp4` (только видеодорожка) | Ошибка "Аудио не найдено" |
| FT-041 | Высокий | Неизвестный MIME type | переименованный `.exe` в `.mp3` | Отклонён на этапе MIME проверки |
| FT-042 | Средний | Несуществующий task_id | GET /result/invalid_id | HTTP 404 "Task not found" |
| FT-043 | Средний | Задача в статусе "processing" | GET /result/{pending_task} | HTTP 202 "ещё обрабатывается" |

### Категория H: UI Функциональность

| ID | Приоритет | Описание | Действие | Ожидаемый результат |
|----|-----------|----------|----------|---------------------|
| FT-044 | Критичный | Drag & drop файла | Перетащить MP3 на зону загрузки | Файл загружен, прогресс-бар появился |
| FT-045 | Критичный | Клик на зону загрузки | Клик → диалог выбора файла | Нативный file picker открылся |
| FT-046 | Высокий | Прогресс-бар при загрузке | Загрузка 10 МБ файла | Прогресс обновляется, показаны % и скорость |
| FT-047 | Высокий | Кнопка "Копировать" | Клик после завершения транскрибации | Текст скопирован в clipboard, уведомление показано |
| FT-048 | Высокий | Кнопка "Скачать TXT" | Клик на готовой задаче | TXT файл скачан с корректным именем |
| FT-049 | Средний | Чекбокс "Литературный текст" | Включить → загрузить файл | Опция передана в API |
| FT-050 | Средний | Чекбокс "Очистить от слов-паразитов" | Включить → загрузить файл | Опция применена к результату |
| FT-051 | Средний | Просмотр истории | Скролл вниз к разделу "История" | Показаны последние задачи |
| FT-052 | Низкий | Обновление истории | Завершить задачу → посмотреть историю | Новая задача добавлена в список |

### Категория I: Edge Cases

| ID | Приоритет | Описание | Входные данные | Ожидаемый результат |
|----|-----------|----------|----------------|---------------------|
| FT-053 | Высокий | Unicode имя файла | `тест_файл_🎤.mp3` | Имя санитизировано, файл обработан |
| FT-054 | Высокий | Очень длинное имя | `a`*300 + `.mp3` | Обрезано до 200 символов |
| FT-055 | Средний | Файл без расширения | `audiofile` (MP3 content) | Определён по MIME type |
| FT-056 | Средний | Несколько точек в имени | `my.audio.test.mp3` | Расширение определено корректно |
| FT-057 | Средний | Тишина в файле | `silence.mp3` (5 сек тишины) | Пустой текст или "(тишина)" |
| FT-058 | Низкий | Одновременная загрузка 3 файлов | 3 параллельных запроса | Все в очереди, обрабатываются по 3 |
| FT-059 | Низкий | Rapid повторная загрузка | Тот же файл 5 раз подряд | Каждый создаёт отдельную задачу |

---

## 4. Тестовые Данные

### Необходимые файлы:

**Аудио (основные):**
- `test_short.mp3` — 1 МБ, 30 сек, русская речь, качество хорошее
- `test_dictation.aac` — 230 КБ, 7 сек, с мобильного диктофона
- `test_long.wav` — 10 МБ, 3 минуты, несжатый

**Аудио (разные форматы):**
- `test.flac`, `test.opus`, `test.m4a`, `test.amr`, `test.wma`

**Видео:**
- `video_interview.mp4` — 10 МБ, с чёткой аудио-дорожкой
- `video_silent.mp4` — видео БЕЗ аудио (для FT-040)
- `video.mkv`, `video.avi`, `video.mov`, `video.webm`

**Большие файлы:**
- `large_30mb.mp3` — 30 МБ
- `large_50mb.wav` — 50 МБ  
- `huge_100mb.mp4` — 100 МБ
- `max_499mb.mp4` — 499 МБ

**Невалидные:**
- `empty.mp3` — 0 байт
- `too_large.mp4` — 501 МБ
- `document.txt` — текстовый файл
- `corrupted.mp3` — битые байты
- `fake.mp3` — `.exe` переименованный в `.mp3`

### Генерация тестовых файлов:

```bash
# Короткий MP3 (синусоида 440 Hz, 30 сек)
ffmpeg -f lavfi -i "sine=frequency=440:duration=30" -ac 1 -ar 16000 test_short.mp3

# WAV несжатый (5 минут)
ffmpeg -f lavfi -i "sine=frequency=440:duration=300" test_long.wav

# Видео с аудио (10 МБ)
ffmpeg -f lavfi -i "testsrc=duration=60:size=640x480:rate=30" \
       -f lavfi -i "sine=frequency=440:duration=60" \
       -c:v libx264 -c:a aac video_interview.mp4

# Тишина
ffmpeg -f lavfi -i "anullsrc=duration=5" silence.mp3

# Пустой файл
touch empty.mp3

# Битый файл
echo "NOT_VALID_MP3_DATA" > corrupted.mp3
```

---

## 5. Тестовое Окружение

**Сервер:**
- URL: `http://92.51.36.233:8000`
- VPS: 2 CPU, 9.7 GB RAM, 38 GB disk
- Docker container: `hearyou-stt`
- Workers: 3 параллельных

**Клиент (для тестирования):**
- curl / wget — API тесты
- Браузер (Chrome/Firefox) — UI тесты
- Bash scripts — автоматизация

**Доступ:**
- SSH: `root@92.51.36.233` (ключ: `aquilla@openclaw`)
- Логи: `docker-compose logs -f` или `GET /logs`

---

## 6. Entry/Exit Criteria

### Entry (Начало тестирования):
✅ Сервис задеплоен и доступен на 92.51.36.233:8000  
✅ API endpoints отвечают (health check passed)  
✅ Тестовые файлы подготовлены  
✅ Тестовый скрипт `test_service.sh` запускается  

### Exit (Завершение тестирования):
✅ **Критичные:** 100% пройдено (FT-001 до FT-023, FT-031-034, FT-036-039, FT-044-045)  
✅ **Высокие:** ≥95% пройдено  
✅ **Средние:** ≥85% пройдено  
✅ **Низкие:** ≥70% пройдено  
✅ Все найденные баги задокументированы  
✅ Регрессионные тесты для исправлений пройдены  

---

## 7. Методология Тестирования

### Ручное тестирование:
- UI flow (FT-044 до FT-052)
- Edge cases (FT-053 до FT-059)
- Визуальная проверка результатов

### Автоматизированное:
- API endpoints (curl scripts)
- Валидация форматов
- Большие файлы (chunked upload)

### Существующие скрипты:
```bash
./test_service.sh          # Базовые проверки (13 тестов)
./test_audio_files.sh      # Реальные аудио (MP3/WAV/AAC/MP4)
```

---

## 8. Расписание

| Этап | Длительность | Ответственный |
|------|--------------|---------------|
| Подготовка данных | 1 час | Аквилла |
| Автоматизированные тесты | 30 минут | Скрипты |
| Ручное тестирование UI | 1 час | Аквилла |
| Тестирование больших файлов | 2 часа | Аквилла |
| Регрессионное тестирование | 30 минут | Скрипты |
| Документация результатов | 30 минут | Аквилла |
| **ИТОГО** | **5.5 часов** | |

---

## 9. Критерии Приёмки

**Блокирующие дефекты (не допускаются):**
- ❌ Сервис недоступен (HTTP 500/502/503)
- ❌ Критичные форматы не работают (MP3, AAC, MP4)
- ❌ Chunked upload ломает файлы
- ❌ Data loss (загруженный файл потерян)
- ❌ Результат транскрибации полностью неверный

**Допустимые (можно отложить):**
- ⚠️ Редкие форматы (AMR, WMA) работают с багами
- ⚠️ UI визуальные глюки (не влияют на функцию)
- ⚠️ Медленная обработка (если работает)

---

## 10. Риски и Митигация

| Риск | Вероятность | Влияние | Митигация |
|------|-------------|---------|-----------|
| Yandex API недоступен | Низкая | Высокое | Mock API для тестов |
| VPS перегрузка при больших файлах | Средняя | Среднее | Тест в off-peak часы |
| Тестовые файлы занимают много места | Высокая | Низкое | Генерировать → тестировать → удалять |
| Длительность тестов (5+ часов) | Высокая | Среднее | Приоритизация критичных |

---

## 11. Что Делать Дальше?

### Опция A: Запустить существующие тесты
```bash
cd /root/hearyou/packages/stt-service
./test_service.sh
./test_audio_files.sh
```

### Опция B: Создать расширенный автоматизированный скрипт
```bash
./test_functional.sh  # Покрытие всех 59 тест-кейсов
```

### Опция C: Ручное пошаговое тестирование
Следовать плану, документировать каждый результат

---

**Готов к исполнению?** Скажи что делать.
