# Очистка временных файлов в Object Storage

## Проблема

Yandex SpeechKit Async API требует загружать аудио файлы в Object Storage перед обработкой. **Файлы не удалялись автоматически** → копился мусор в S3 бакете `hearyou-stt-temp`.

---

## Решение

### ✅ Автоматическое удаление (реализовано)

**Файл:** `packages/stt-service/app.py`

После получения результата транскрибации файл автоматически удаляется из S3:

```python
# Удаляем временный файл из Object Storage
try:
    object_name = Path(audio_to_send).name
    stt.delete_from_storage(object_name)
    logger.info(f"Task {task_id}: cleaned up S3 file {object_name}")
except Exception as e:
    logger.warning(f"Task {task_id}: failed to cleanup S3 file: {e}")
```

**Workflow:**
1. Загрузка аудио в S3 → `06f3f800b382eadb.ogg`
2. Yandex обрабатывает файл
3. `wait_for_completion()` получает результат
4. **✅ Файл удаляется из S3**

---

## Новый метод с автоочисткой

**Файл:** `packages/stt-yandex/yandex_stt.py`

Добавлен метод `transcribe_async_with_cleanup()`:

```python
result = stt.transcribe_async_with_cleanup(
    "audio.mp3",
    language="ru-RU",
    punctuation=True,
    timeout=600
)
# Файл автоматически удаляется после получения результата
```

Внутри метода:
- `try:` → загрузка, транскрибация, ожидание
- `finally:` → удаление файла (даже при ошибках)

---

## Очистка старых файлов

### Скрипт cleanup-s3-old-files.sh

**Файл:** `scripts/cleanup-s3-old-files.sh`

Удаляет **все** файлы из бакета `hearyou-stt-temp`:

```bash
chmod +x /root/hearyou/scripts/cleanup-s3-old-files.sh
/root/hearyou/scripts/cleanup-s3-old-files.sh
```

**Что делает:**
1. Показывает список файлов
2. Спрашивает подтверждение
3. Удаляет все файлы через AWS CLI
4. Показывает статистику

**Использование:**

```bash
$ ./cleanup-s3-old-files.sh

🗑️  Очистка старых файлов из Object Storage...
📦 Bucket: hearyou-stt-temp

📋 Список файлов:
2026-02-27 16:12:00     42.37 KB  06f3f800b382eadb.ogg
2026-02-27 16:18:00     35.52 KB  29d42bbd79bddd7d.ogg
...

❓ Удалить ВСЕ файлы из bucket? (y/N): y

🗑️  Удаление...
delete: s3://hearyou-stt-temp/06f3f800b382eadb.ogg
delete: s3://hearyou-stt-temp/29d42bbd79bddd7d.ogg
...

✅ Готово!

📊 Статистика:
Осталось файлов: 0
```

---

## Lifecycle Policy (опционально)

Дополнительная защита от мусора - автоудаление через N дней.

**Файл:** `scripts/setup-s3-lifecycle.sh`

```bash
chmod +x /root/hearyou/scripts/setup-s3-lifecycle.sh
/root/hearyou/scripts/setup-s3-lifecycle.sh
```

**Что делает:**
- Создаёт Lifecycle Policy для бакета
- Файлы старше 1 дня автоматически удаляются

**Policy:**
```json
{
  "Rules": [{
    "ID": "DeleteTempFiles",
    "Status": "Enabled",
    "Filter": {"Prefix": ""},
    "Expiration": {"Days": 1}
  }]
}
```

**Ручная настройка** (если скрипт не работает):

1. Открыть [Yandex Cloud Console](https://console.cloud.yandex.ru/)
2. Object Storage → Бакеты → `hearyou-stt-temp`
3. Настройки → Lifecycle
4. Добавить правило:
   - Имя: "Auto-delete temp files"
   - Префикс: (пусто)
   - Удаление объектов через: **1 день**
5. Сохранить

---

## Статистика S3

### Проверка текущего использования

**Web-интерфейс:**
```
https://console.cloud.yandex.ru/folders/<folder-id>/storage/buckets/hearyou-stt-temp
```

**AWS CLI:**
```bash
source /root/hearyou/packages/stt-service/.env.yandex

aws s3 ls s3://hearyou-stt-temp/ \
  --endpoint-url=https://storage.yandexcloud.net \
  --summarize --recursive

# Вывод:
# Total Objects: 6
# Total Size: 181.06 KB
```

### Стоимость хранения

**Yandex Object Storage тарифы:**
- Хранение: ~2.5₽ за ГБ/месяц
- PUT/DELETE: бесплатно (первые 10,000 в месяц)

**Наш случай:**
- Без автоочистки: ~10 MB/день = ~300 MB/месяц = **~0.75₽/месяц**
- С автоочисткой: ~1 MB (несколько активных файлов) = **~0.002₽/месяц**

Экономия мизерная, но порядок важен! 🧹

---

## Тестирование

### Тест 1: Загрузка и автоудаление

```bash
# Загрузить файл
curl -X POST https://92.51.36.233/transcribe \
  -F "file=@test.mp3" \
  -F "speaker_labeling=false"

# Получить task_id: abc123...

# Проверить S3 ДО обработки
aws s3 ls s3://hearyou-stt-temp/ --endpoint-url=https://storage.yandexcloud.net
# Должен быть файл: 06f3f800b382eadb.ogg

# Дождаться завершения транскрипции (проверить /status/abc123)

# Проверить S3 ПОСЛЕ обработки
aws s3 ls s3://hearyou-stt-temp/ --endpoint-url=https://storage.yandexcloud.net
# Файл должен исчезнуть ✅
```

### Тест 2: Удаление при ошибке

```bash
# Симуляция ошибки (невалидный аудио файл)
curl -X POST https://92.51.36.233/transcribe \
  -F "file=@broken.txt" \
  -F "speaker_labeling=false"

# Проверить S3
aws s3 ls s3://hearyou-stt-temp/ --endpoint-url=https://storage.yandexcloud.net

# Файл должен быть удалён даже при ошибке (благодаря try/finally) ✅
```

---

## Логи

**Проверка удаления в логах:**

```bash
docker logs hearyou-stt | grep "cleaned up S3"
```

**Пример вывода:**
```
2026-02-27 16:30:45 [INFO] app: Task abc123: cleaned up S3 file 06f3f800b382eadb.ogg
2026-02-27 16:35:12 [INFO] app: Task def456: cleaned up S3 file 29d42bbd79bddd7d.ogg
```

**Ошибки удаления:**
```bash
docker logs hearyou-stt | grep "failed to cleanup S3"
```

---

## Итоги

### До изменений ❌
- Файлы копились в S3 бесконечно
- 181 KB за 1 день → ~5.4 MB/месяц
- Нужна была ручная очистка

### После изменений ✅
- Файлы удаляются автоматически после обработки
- S3 бакет почти пуст (только активные задачи)
- Lifecycle Policy как дополнительная защита

### Файлы изменены:
- ✅ `packages/stt-yandex/yandex_stt.py` - метод `transcribe_async_with_cleanup()`
- ✅ `packages/stt-service/app.py` - автоудаление после `wait_for_completion()`
- ✅ `scripts/cleanup-s3-old-files.sh` - ручная очистка старых файлов
- ✅ `scripts/setup-s3-lifecycle.sh` - настройка Lifecycle Policy

---

## Рекомендации

1. ✅ **Код уже обновлён** - новые файлы удаляются автоматически
2. 🧹 **Запустить cleanup** - удалить старые файлы вручную один раз
3. ⚙️ **Настроить Lifecycle** (опционально) - дополнительная защита

```bash
# Разовая очистка старых файлов
/root/hearyou/scripts/cleanup-s3-old-files.sh

# Lifecycle Policy (опционально)
/root/hearyou/scripts/setup-s3-lifecycle.sh
```

**Готово!** 🎉
