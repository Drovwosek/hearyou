# 🎯 Улучшение качества транскрипции

Руководство по настройке фильтров для получения максимально чистого текста.

## 🔧 Доступные фильтры

### 1. **Лишние звуки** (всегда включено)

Удаляет:
- Растянутые звуки: `эээ`, `ммм`, `ааа`, `ууу`
- Повторяющиеся слоги: `а-а-а`, `э-э-э`, `м-м-м`
- Междометия: `эм`, `ам`, `бе`, `ме`, `хм`, `гм`, `кхм`

**Пример:**
```
До:  Эээ, ммм, бе ме, я хотел сказать
После: Я хотел сказать
```

### 2. **Слова-паразиты** (опция `clean=true`)

Удаляет:
- Классика: `вот`, `ну`, `короче`, `типа`, `значит`
- Связки: `в общем`, `то есть`, `это самое`, `так сказать`
- Молодёжный сленг: `ваще`, `кароч`, `прям`, `буквально`, `реально`

**Пример:**
```
До:  Ну вот, короче, типа всё готово
После: Всё готово
```

⚠️ **Внимание:** Агрессивная очистка может удалить полезные слова! Используйте когда важна краткость.

### 3. **Артефакты транскрибации** (опция `corrections=true`, по умолчанию включено)

Исправляет частые ошибки STT:

| Что слышит STT | Что должно быть |
|----------------|-----------------|
| иришка, иишка  | ИИшка          |
| мелишка        | милишка        |
| испринг, ай спринг | iSpring    |
| спринт         | СПРИНТ         |
| егэ            | ЕГЭ            |
| огэ            | ОГЭ            |
| лмс            | LMS            |
| апи            | API            |
| айпи           | IP             |

**Пример:**
```
До:  Мы используем испринг и лмс для егэ
После: Мы используем iSpring и LMS для ЕГЭ
```

## 📋 Режимы работы

### Режим 1: Базовый (по умолчанию)
```
literature=true, clean=false, corrections=true
```

✅ Что делает:
- Yandex убирает "эээ", "ммм" на уровне API (literature_text)
- Дополнительно убирает "бе", "ме", повторы (а-а-а)
- Исправляет артефакты (иишка → ИИшка)
- **НЕ** удаляет слова-паразиты

📝 Для кого: обычные интервью, встречи, casual разговоры

**Пример:**
```
До:  Эээ, бе ме, ну вот, мы используем испринг для егэ
После: Ну вот, мы используем iSpring для ЕГЭ
```

### Режим 2: Агрессивный (чистый текст)
```
literature=true, clean=true, corrections=true
```

✅ Что делает:
- Всё из Режима 1
- **+** Удаляет слова-паразиты

📝 Для кого: конспекты лекций, статьи, официальные документы

**Пример:**
```
До:  Эээ, ну вот, короче, типа, мы используем испринг для егэ
После: Мы используем iSpring для ЕГЭ
```

### Режим 3: Минимальный (сырой текст)
```
literature=false, clean=false, corrections=false
```

✅ Что делает:
- Ничего! Оставляет текст как есть от Yandex API

📝 Для кого: когда нужна максимальная точность (судебные записи, стенограммы)

## 🛠️ Добавление своих коррекций

### Через JSON файл (рекомендуется)

Создай файл `ispring_corrections.json` в `/packages/stt-service/`:

```json
{
  "_comment": "Дополнительные исправления для вашего проекта",
  
  "names": {
    "иван": "Иван",
    "петр": "Пётр"
  },
  
  "products": {
    "ваш_продукт": "Ваш Продукт"
  },
  
  "technical": {
    "термин": "Термин"
  }
}
```

### Программно (для разработчиков)

Отредактируй `core/stt_corrections.py`:

```python
self.corrections = {
    "неправильно": "Правильно",
    "фамилия": "Фамилия",
}
```

Для слов-паразитов отредактируй `core/filler_words_filter.py`:

```python
self.russian_fillers.append(r'\bмоё_слово\b')
```

## 📊 Сравнение качества

### Исходный текст (STT без фильтров):
> Эээ, бе ме, ну вот, короче, я хотел рассказать про иишка и испринг. Ммм, типа это самое, мы используем лмс и апи для спринт. А-а-а, егэ и огэ ваще сложные.

### Базовый режим (literature=true, clean=false, corrections=true):
> Ну вот, короче, я хотел рассказать про ИИшка и iSpring. Типа это самое, мы используем LMS и API для СПРИНТ. ЕГЭ и ОГЭ ваще сложные.

### Агрессивный режим (literature=true, clean=true, corrections=true):
> Я хотел рассказать про ИИшка и iSpring. Мы используем LMS и API для СПРИНТ. ЕГЭ и ОГЭ сложные.

## ⚙️ Технические детали

### Порядок обработки:

1. **Yandex API** → `literature_text=True` (убирает большую часть звуков)
2. **FillerWordsFilter** → Дополнительно удаляет лишние звуки (бе, ме) и слова-паразиты
3. **TranscriptionCorrector** → Исправляет артефакты по словарю + ispring_corrections.json

### Производительность:

- Добавляет ~0.1-0.3 сек на обработку текста
- Не влияет на скорость транскрибации (применяется после получения результата)
- Работает полностью локально

### Архитектура:

```
TranscriptionCleaner (text_cleaner.py)
├── FillerWordsFilter (filler_words_filter.py)
│   ├── Удаление звуков (эээ, ммм, бе, ме)
│   └── Удаление паразитов (вот, типа, короче)
└── TranscriptionCorrector (stt_corrections.py)
    ├── Базовые коррекции (иишка → ИИшка)
    └── ispring_corrections.json (кастомные правила)
```

## 🎓 Рекомендации

| Тип контента | Режим | Параметры | Почему |
|--------------|-------|-----------|---------|
| Интервью | Базовый | `literature=true, clean=false` | Сохраняет естественность речи |
| Лекция | Агрессивный | `literature=true, clean=true` | Убирает лишнее, оставляет суть |
| Встреча | Базовый | `literature=true, clean=false` | Контекст важен |
| Стенограмма | Минимальный | `literature=false, clean=false, corrections=false` | Максимальная точность |
| Конспект | Агрессивный | `literature=true, clean=true` | Краткость + читаемость |

## 📝 Примеры использования

### Через Web UI:

1. Загрузи файл
2. Отметь опции:
   - ✅ **Литературный текст** (literature) - всегда рекомендуется
   - ✅ **Убрать слова-паразиты** (clean) - для чистого текста
   - ✅ **Применять исправления** (corrections) - для артефактов (иишка)

### Через Python API:

```python
from core.text_cleaner import clean_transcription

text = "Эээ ну вот иришка это испринг"

# Базовая очистка
clean_text = clean_transcription(
    text,
    remove_filler_sounds=True,
    remove_filler_words=False,
    fix_artifacts=True
)
# → "Ну вот ИИшка это iSpring"

# Агрессивная очистка
super_clean = clean_transcription(
    text,
    remove_filler_sounds=True,
    remove_filler_words=True,
    fix_artifacts=True
)
# → "ИИшка iSpring"
```

## 🚀 Changelog

- **2026-02-28:** 
  - Добавлен полный пайплайн `TranscriptionCleaner`
  - Расширен `FillerWordsFilter` (бе, ме, повторы)
  - Расширен словарь артефактов (iSpring, LMS, API, ЕГЭ)
  - Документация

---

💡 **Совет:** Начни с Базового режима, а потом переключись на Агрессивный если нужен более чистый текст.

🐛 **Нашли ошибку транскрипции?** Добавьте её в `ispring_corrections.json` и создайте Pull Request!
