# 🎭 Speaker Diarization - Реализация

## Архитектура решения

### Гибридный подход: Yandex STT + pyannote.audio

```
┌──────────────────────────────────────────────────────┐
│ Пользователь загружает файл + ✓ Определить спикеров │
└─────────────────┬────────────────────────────────────┘
                  │
        ┌─────────┴─────────┐
        │                   │
        ▼                   ▼
┌─────────────────┐  ┌──────────────────┐
│ Yandex STT      │  │ pyannote.audio   │
│ (транскрипция)  │  │ (диаризация)     │
│                 │  │                  │
│ Качество: ⭐⭐⭐⭐⭐│  │ Точность: ⭐⭐⭐⭐  │
│ Скорость: 10 сек│  │ Скорость: 30-60с │
│ Стоимость: 1.2₽ │  │ Стоимость: 0₽    │
└────────┬────────┘  └────────┬─────────┘
         │                    │
         │   Параллельно      │
         └─────────┬──────────┘
                   ▼
          ┌────────────────┐
          │ Объединение    │
          │ по timestamps  │
          └────────┬───────┘
                   ▼
          ┌────────────────┐
          │ Текст с метками│
          │ спикеров       │
          └────────────────┘
```

---

## Компоненты

### 1. speaker_diarization.py

**Класс SpeakerDiarization:**
- Инициализация pyannote pipeline (ленивая)
- Метод `diarize()` → возвращает сегменты спикеров

**Функция merge_transcription_with_speakers:**
- Объединяет слова от Yandex с метками спикеров
- Matching по timestamps

**Функция format_with_speakers:**
- Форматирует текст как диалог
- Группирует реплики по спикерам

---

### 2. app.py (модифицированный)

**Изменения:**
1. Импорт `SpeakerDiarization`
2. Инициализация `diarizer = SpeakerDiarization()`
3. Параллельный запуск Yandex + pyannote
4. Объединение результатов

**Код (упрощённо):**

```python
if options.get("speaker_labeling"):
    # Запускаем параллельно
    yandex_task = asyncio.create_task(yandex_transcription())
    diarization_task = asyncio.create_task(diarizer.diarize(audio_file))
    
    # Ждём оба результата
    yandex_result, speaker_segments = await asyncio.gather(
        yandex_task,
        diarization_task
    )
    
    # Объединяем
    words_with_speakers = merge_transcription_with_speakers(
        yandex_result['words'],
        speaker_segments
    )
    
    text = format_with_speakers(words_with_speakers)
```

---

### 3. yandex_stt.py (откат изменений)

**Что откатить:**
- `audioChannelCount: 2` → `audioChannelCount: 1` (моно)
- `rawResults: True` → убрать
- `model: phone_call` → `model: general`
- `speakerLabeling: true` → убрать (не используем)

**Почему:**
- Yandex v2 не поддерживает speaker labeling
- Диаризацию делаем через pyannote
- Моно быстрее и дешевле

---

## Производительность

### Файл 10 минут:

| Этап | Время | Прогресс |
|------|-------|----------|
| Загрузка | 1-2с | 0-10% |
| FFmpeg конвертация | 2-3с | 10-30% |
| Upload в S3 | 3-5с | 30-50% |
| **Yandex STT** | **~10с** | **50-70%** |
| **pyannote diarization** | **~30-60с** | **70-80%** |
| Объединение | 1-2с | 80-85% |
| Фильтрация/corrections | 1-2с | 85-95% |
| Сохранение | 1с | 95-100% |
| **ИТОГО** | **~60-80 сек** | |

**С диаризацией:** ~60-80 сек  
**Без диаризации:** ~20-30 сек

**Замедление:** ~40-50 секунд на файл 10 минут → **приемлемо**

---

## Стоимость

| Компонент | Стоимость |
|-----------|-----------|
| Yandex STT | 1.2₽/мин |
| pyannote.audio | 0₽ (локально) |
| S3 storage | ~0.01₽ (временно) |
| **ИТОГО** | **~1.2₽/мин** |

**Без изменений!** Диаризация бесплатна.

---

## Качество

### Yandex STT:
- WER: 5-8% для русского
- Лучший в России
- Hints/corrections работают

### pyannote.audio:
- Точность диаризации: ~85-92%
- Ошибки при похожих голосах
- Шум может повлиять

### Объединённый результат:
- Качество текста: ⭐⭐⭐⭐⭐ (Yandex)
- Разметка спикеров: ⭐⭐⭐⭐ (pyannote)

---

## Зависимости

```txt
torch>=2.0.0          # ~1.2GB
torchaudio>=2.0.0     # ~50MB
pyannote.audio>=3.1.0 # ~10MB + модели ~290MB
```

**Размер на диске:** ~1.5GB

**HuggingFace токен:** Требуется (бесплатный)

---

## UI

**Checkbox остаётся:**
```html
<input type="checkbox" name="speaker_labeling" id="speaker_labeling">
<label for="speaker_labeling">🎭 Определить спикеров</label>
```

**Теперь работает!**

---

## Пример результата

**Входной аудио:** Интервью 2 человека, 5 минут

**Результат:**

```
SPEAKER_00: Здравствуйте, расскажите о вашем проекте.

SPEAKER_01: Здравствуйте! Мы разработали систему для автоматической 
транскрибации аудио с определением спикеров. Она использует Yandex 
SpeechKit для распознавания текста и pyannote для определения кто говорит.

SPEAKER_00: Интересно! А какое качество распознавания?

SPEAKER_01: Для русского языка WER составляет около 5-8 процентов, 
это лучший результат среди доступных решений. Плюс мы добавили систему 
исправлений для терминов.

SPEAKER_00: Отлично! Сколько это стоит?

SPEAKER_01: Около одного рубля двадцать копеек за минуту аудио. 
Диаризация бесплатная, так как работает локально.
```

---

## Тестирование

### Тест 1: Базовая работоспособность
```bash
# Загрузить файл с 1 спикером
curl -k -F "file=@test.mp3" -F "speaker_labeling=true" \
  https://92.51.36.233/transcribe
```

**Ожидаемо:** 1 спикер (SPEAKER_00), весь текст ему принадлежит

### Тест 2: Два спикера
```bash
# Загрузить файл с 2+ спикерами
curl -k -F "file=@interview.mp3" -F "speaker_labeling=true" \
  https://92.51.36.233/transcribe
```

**Ожидаемо:** 2+ спикеров, текст разбит на реплики

### Тест 3: Производительность
- Файл 10 минут
- Измерить время обработки
- Проверить что < 2 минут

---

## Известные ограничения

1. **Похожие голоса:** pyannote может путать похожих спикеров
2. **Шум:** Фоновый шум снижает точность диаризации
3. **Перекрытия:** Когда говорят одновременно - может ошибаться
4. **CPU-bound:** На CPU медленнее (30-60с), на GPU быстрее (~10с)

**Решения:**
- Для GPU: добавить CUDA support
- Для качества: preprocessing (шумоподавление)
- Для скорости: batch processing

---

## Roadmap

### v1.0 (текущая реализация)
- ✅ Yandex STT
- ✅ pyannote.audio CPU
- ✅ Базовое объединение
- ✅ UI готов

### v1.1 (опционально)
- [ ] GPU support для pyannote
- [ ] Шумоподавление (preprocessing)
- [ ] Настройка количества спикеров
- [ ] Именование спикеров (Interviewer / Respondent)

### v2.0 (будущее)
- [ ] Real-time streaming diarization
- [ ] Эмоциональная окраска
- [ ] Экспорт в разные форматы (SRT, VTT)

---

## Заключение

**Гибридный подход Yandex + pyannote:**
- ✅ Лучшее качество текста (Yandex)
- ✅ Работающая диаризация (pyannote)
- ✅ Без VPN
- ✅ Бесплатная диаризация
- ✅ Та же стоимость (1.2₽/мин)
- ⚠️ Чуть медленнее (+40-50 сек на 10 мин файл)

**Статус:** Готово к внедрению 🚀
