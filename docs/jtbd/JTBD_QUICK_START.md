# JTBD Analyzer — Quick Start 🚀

## TL;DR

JTBD анализатор интегрирован в HearYou и работает **автоматически** после транскрибации.

**1 команда для установки:**

```bash
cd packages/stt-service && pip install -r requirements.txt
export ANTHROPIC_API_KEY="your-key"
python app.py
```

**1 запрос для использования:**

```bash
curl -X POST "http://localhost:8000/transcribe" \
  -F "file=@interview.mp3" \
  -F "analyze_jtbd=true"
```

**Результат:** JSON с Jobs, Pains, Gains, Context, Triggers + цитатами из транскрипции.

---

## Что вы получаете

### Входные данные:
Аудио файл интервью (MP3, WAV, etc.)

### Выходные данные:

```json
{
  "text": "Я хочу научиться программировать, но нет времени...",
  "jtbd": {
    "jobs": [
      {
        "text": "Научиться программированию на Python",
        "quote": "быстро освоить Python",
        "type": "functional",
        "confidence": "high"
      }
    ],
    "pains": [
      {
        "text": "Нехватка времени для длинных курсов",
        "quote": "у меня нет времени на длинные курсы",
        "severity": "high"
      }
    ],
    "gains": [
      {
        "text": "Интерактивная обратная связь",
        "quote": "платформа где можно сразу писать код и получать обратную связь",
        "type": "desired"
      }
    ],
    "summary": "Пользователь хочет быстро освоить Python..."
  }
}
```

---

## Примеры использования

### 1. Customer Development Interview

**Вход:** Запись интервью с потенциальным клиентом (30 мин)

**Выход:** Структурированные Jobs, Pains, Gains для валидации гипотез

**Стоимость:** ~$0.05 за интервью

### 2. User Research Session

**Вход:** Скрин-запись usability тестирования с комментариями

**Выход:** Контекст использования + триггеры + боли

### 3. Sales Call Analysis

**Вход:** Запись звонка менеджера с клиентом

**Выход:** Возражения (Pains) + триггеры покупки + желаемые выгоды

---

## Файлы проекта

```
hearyou/
├── core/
│   └── jtbd_analyzer.py         ← Основной модуль
├── packages/stt-service/
│   ├── app.py                   ← Интеграция (изменён)
│   └── requirements.txt         ← Зависимости (anthropic добавлен)
├── JTBD_README.md               ← Полная документация
├── JTBD_EXAMPLE.json            ← Пример результата
├── JTBD_INTEGRATION.md          ← Инструкция по интеграции
└── JTBD_QUICK_START.md          ← Этот файл
```

---

## Основные методы

### Python API

```python
from core.jtbd_analyzer import JTBDAnalyzer

# Инициализация
analyzer = JTBDAnalyzer()

# Анализ
result = analyzer.analyze("текст транскрипции...")

# Форматирование
markdown = analyzer.format_as_markdown(result)
print(markdown)
```

### REST API

```bash
# Транскрибация + JTBD
POST /transcribe
  - file: аудио файл
  - analyze_jtbd: true  # по умолчанию

# Получение результата
GET /result/{task_id}
  → { "text": "...", "jtbd": {...} }
```

---

## Настройки

| Параметр | Значение по умолчанию | Описание |
|----------|----------------------|----------|
| `analyze_jtbd` | `True` | Включить JTBD анализ |
| `model` | `claude-sonnet-4-5-20250929` | Модель Claude |
| `max_tokens` | `4000` | Макс токенов для ответа |

**Отключить анализ:**

```bash
curl -F "analyze_jtbd=false" ...
```

**Изменить модель:**

```python
analyzer = JTBDAnalyzer(model="claude-3-5-sonnet-20241022")
```

---

## FAQ

**Q: Сколько стоит?**  
A: ~$0.017 за транскрипцию в 1000 слов. Для 100 интервью ~$1.70.

**Q: Какие языки поддерживаются?**  
A: Все языки, поддерживаемые Claude (ru, en, fr, de, es, и т.д.)

**Q: Можно без Claude API?**  
A: Нет, JTBD анализ требует Claude. Но можно отключить (`analyze_jtbd=false`).

**Q: Как улучшить качество?**  
A: Используйте чистые транскрипции (`clean=True`), добавьте few-shot примеры в промпт.

---

## Логи

```
INFO: JTBDAnalyzer initialized with model: claude-sonnet-4-5-20250929
INFO: Task abc123: starting JTBD analysis
INFO: JTBD analysis completed: 15 elements, 1247 in / 856 out tokens
```

Уровень логирования: `INFO` (измените на `DEBUG` для детальных логов)

---

## Roadmap (опционально)

- [ ] UI для визуализации JTBD (drag-drop элементов)
- [ ] Агрегация нескольких интервью
- [ ] Экспорт в PDF/Notion/Miro
- [ ] Интеграция с CRM системами
- [ ] Sentiment analysis для каждого элемента

---

**Вопросы?** См. [JTBD_README.md](./JTBD_README.md) для полной документации.

**Примеры?** См. [JTBD_EXAMPLE.json](./JTBD_EXAMPLE.json) для JSON структуры.

**Интеграция?** См. [JTBD_INTEGRATION.md](./JTBD_INTEGRATION.md) для деталей.
